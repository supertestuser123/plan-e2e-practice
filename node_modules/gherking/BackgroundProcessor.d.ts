import { Background, Feature, Rule } from "gherkin-ast";
import { PreCompiler, SingleControlType } from "./PreCompiler";
import { PartialProcessor } from "./Processor";
export declare class BackgroundProcessor<P extends Feature | Rule> extends PartialProcessor<Background, P> {
    private stepProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    preFilter(e: Background, p: P): Promise<boolean>;
    postFilter(e: Background, p: P): Promise<boolean>;
    process(e: Background, p: P): Promise<SingleControlType<Background>>;
}
