import { DataTable, Step } from "gherkin-ast";
import { PreCompiler, SingleControlType } from "./PreCompiler";
import { Processor } from "./Processor";
export declare class DataTableProcessor extends Processor<DataTable, Step> {
    private tableRowProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: DataTable, p: Step): Promise<boolean>;
    protected postFilter(e: DataTable, p: Step): Promise<boolean>;
    protected process(e: DataTable, p: Step): Promise<SingleControlType<DataTable>>;
}
