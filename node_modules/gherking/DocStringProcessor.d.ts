import { DocString, Step } from "gherkin-ast";
import { SingleControlType } from "./PreCompiler";
import { Processor } from "./Processor";
export declare class DocStringProcessor extends Processor<DocString, Step> {
    protected preFilter(e: DocString, p: Step): Promise<boolean>;
    protected postFilter(e: DocString, p: Step): Promise<boolean>;
    protected process(e: DocString, p: Step): Promise<SingleControlType<DocString>>;
}
