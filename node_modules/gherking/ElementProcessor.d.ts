import { Feature, Rule, Element } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class ElementProcessor<P extends Feature | Rule> extends ListProcessor<Element, P> {
    private backgroundProcessor;
    private scenarioProcessor;
    private scenarioOutlineProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: Element, p: P, i: number): Promise<boolean>;
    protected postFilter(e: Element, p: P, i: number): Promise<boolean>;
    protected process(e: Element, p: P, i: number): Promise<MultiControlType<Element>>;
}
