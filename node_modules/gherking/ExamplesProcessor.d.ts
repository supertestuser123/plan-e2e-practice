import { Examples, ScenarioOutline } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class ExamplesProcessor extends ListProcessor<Examples, ScenarioOutline> {
    private tagsProcessor;
    private tableRowProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: Examples, p: ScenarioOutline, i: number): Promise<boolean>;
    protected postFilter(e: Examples, p: ScenarioOutline, i: number): Promise<boolean>;
    protected process(e: Examples, p: ScenarioOutline, i: number): Promise<MultiControlType<Examples>>;
    private postProcess;
}
