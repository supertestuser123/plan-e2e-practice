import { Document, Feature } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { Processor } from "./Processor";
export declare class FeatureProcessor extends Processor<Feature, Document, MultiControlType<Feature>> {
    private tagProcessor;
    private ruleProcessor;
    private elementProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: Feature, p: Document): Promise<boolean>;
    protected postFilter(e: Feature, p: Document): Promise<boolean>;
    protected process(e: Feature, p: Document): Promise<Feature[]>;
    private hasRule;
}
