import { Feature, Rule } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class RuleProcessor extends ListProcessor<Rule, Feature> {
    private elementProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: Rule, p: Feature, i: number): Promise<boolean>;
    protected postFilter(e: Rule, p: Feature, i: number): Promise<boolean>;
    protected process(e: Rule, p: Feature, i: number): Promise<MultiControlType<Rule>>;
    private postProcess;
}
