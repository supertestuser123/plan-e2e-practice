import { Feature, Rule, ScenarioOutline, Scenario } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { PartialListProcessor } from "./Processor";
export declare class ScenarioOutlineProcessor<P extends Feature | Rule> extends PartialListProcessor<ScenarioOutline, P, Scenario | ScenarioOutline> {
    private stepProcessor;
    private tagProcessor;
    private examplesProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    preFilter(e: ScenarioOutline, p: P, i: number): Promise<boolean>;
    postFilter(e: ScenarioOutline, p: P, i: number): Promise<boolean>;
    process(e: ScenarioOutline, p: P, i: number): Promise<MultiControlType<Scenario | ScenarioOutline>>;
    private postProcess;
}
