import { Feature, Rule, Scenario } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { PartialListProcessor } from "./Processor";
export declare class ScenarioProcessor<P extends Feature | Rule> extends PartialListProcessor<Scenario, P> {
    private stepProcessor;
    private tagProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    preFilter(e: Scenario, p: P, i: number): Promise<boolean>;
    postFilter(e: Scenario, p: P, i: number): Promise<boolean>;
    process(e: Scenario, p: P, i: number): Promise<MultiControlType<Scenario>>;
    private postProcess;
}
