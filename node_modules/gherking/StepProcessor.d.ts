import { Background, Scenario, ScenarioOutline, Step } from "gherkin-ast";
import { MultiControlType, PreCompiler } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class StepProcessor<P extends Background | Scenario | ScenarioOutline> extends ListProcessor<Step, P> {
    private docStringProcessor;
    private dataTableProcessor;
    constructor(preCompiler?: Partial<PreCompiler>);
    protected preFilter(e: Step, p: P, i: number): Promise<boolean>;
    protected postFilter(e: Step, p: P, i: number): Promise<boolean>;
    protected process(e: Step, p: P, i: number): Promise<MultiControlType<Step>>;
    private postProcess;
}
