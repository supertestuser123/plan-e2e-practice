import { DataTable, Examples, TableRow } from "gherkin-ast";
import { MultiControlType } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class TableRowProcessor<P extends DataTable | Examples> extends ListProcessor<TableRow, P> {
    protected preFilter(e: TableRow, p: P, i: number): Promise<boolean>;
    protected postFilter(e: TableRow, p: P, i: number): Promise<boolean>;
    protected process(e: TableRow, p: P, i: number): Promise<MultiControlType<TableRow>>;
}
