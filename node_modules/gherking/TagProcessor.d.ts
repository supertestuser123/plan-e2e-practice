import { Examples, Feature, Scenario, ScenarioOutline, Tag } from "gherkin-ast";
import { MultiControlType } from "./PreCompiler";
import { ListProcessor } from "./Processor";
export declare class TagProcessor<P extends Feature | Scenario | ScenarioOutline | Examples> extends ListProcessor<Tag, P> {
    protected preFilter(e: Tag, p: P, i: number): Promise<boolean>;
    protected postFilter(e: Tag, p: P, i: number): Promise<boolean>;
    protected process(e: Tag, p: P, i: number): Promise<MultiControlType<Tag>>;
}
